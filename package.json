{
  "name": "word-equation-numbering-addin",
  "version": "0.1.0",
  "private": true,
  "description": "Word Office Add-in for inserting native equation lines with caption-based numbering and cross references.",
  "scripts": {
    "dev": "vite --host localhost",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host localhost"
  },
  "dependencies": {
    "@vitejs/plugin-basic-ssl": "^1.2.0",
    "vite": "^6.3.5",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@types/office-js": "^1.0.535"
  }
}
